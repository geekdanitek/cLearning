#include <stdio.h>

// ++num1 means x = x + 1; pre increment means the increment will be done immediately on the variable
// --num1 means x = x - 1; pre decrement means the decrement will be done immediately on the variable


// num1++ means x = x + 1; post increment means the increment will be done on the next occurence of the variable
// num1-- means x = x - 1; pre decrement means the decrement will be done on the next occurence of the variable


int main(){

    return 0;
}